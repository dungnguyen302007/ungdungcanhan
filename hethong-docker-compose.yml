version: '3'

services:
  hethong:
    image: nginx:alpine
    container_name: hethong_web
    volumes:
      - /var/www/hethong:/usr/share/nginx/html:ro
    restart: unless-stopped
    networks:
      - n8n_default

networks:
  n8n_default:
    external: true
